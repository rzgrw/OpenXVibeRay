# xrRenderMetal — Metal hardware abstraction layer
#
# NOTE: This directory does NOT build a standalone library.
# Its sources are compiled directly into xrRenderPC_Metal
# (the same pattern as xrRenderGL/ sources being listed in
# xrRenderPC_GL/CMakeLists.txt).
#
# This file exists as documentation and a placeholder.
# Sources to include in xrRenderPC_Metal:
#   metalBufferUtils.cpp
#   metalConstantBuffer.cpp
#   metalHW.cpp
#   metalr_constants.cpp
#   metalShaderCompiler.cpp
#   metalState.cpp
#   metalStateUtils.cpp
#   metalVertexInput.cpp
